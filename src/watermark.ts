/** Watermarks for Tezos Blocks. */

/** Block Watermark. */
export const BLOCK = new Uint8Array([1])

/** Endorsement Watermark */
export const ENDORSEMENT = new Uint8Array([2])

/** Operation Watermark. */
export const OPERATION = new Uint8Array([3])
